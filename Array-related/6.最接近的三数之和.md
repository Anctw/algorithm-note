### [6. 最接近的三数之和](https://leetcode.cn/problems/3sum-closest/)

**TODO 优化解法的编程思想**



给你一个长度为 `n` 的整数数组 `nums` 和 一个目标值 `target`。请你从 `nums` 中选出三个在 **不同下标位置** 的整数，使它们的和与 `target` 最接近。

返回这三个数的和。

假定每组输入只存在恰好一个解。

 

**示例 1：**

```
输入：nums = [-1,2,1,-4], target = 1
输出：2
解释：与 target 最接近的和是 2 (-1 + 2 + 1 = 2)。
```

**示例 2：**

```
输入：nums = [0,0,0], target = 1
输出：0
解释：与 target 最接近的和是 0（0 + 0 + 0 = 0）。
```

**提示：**

- `3 <= nums.length <= 1000`
- `-1000 <= nums[i] <= 1000`
- `-104 <= target <= 104`



#### C++

```C++
//暴力解法
class Solution {
public:
    int threeSumClosest(vector<int>& nums, int target) {
        int n = nums.size();
        int result;
        int cha = INT_MAX;
        for(int k = 1; k < n - 1 ; k++){
            for(int l = 0; l < k ; l++){
                for(int r = n- 1; r > k ; r--){
                    int sum =  nums[k] + nums[l] + nums[r];
                    if(abs(sum-target) < cha){
                        cha = abs(sum - target);
                        result = sum;
                    }
                }
            }
        }
        return result;
    }
};

//优化解法
class Solution {
public:
    int threeSumClosest(vector<int>& nums, int target) {
        sort(nums.begin(), nums.end());
        int ans = INT_MAX;
        int n = nums.size();
        for(int i =0;i<n;i++){
            int a = nums[i];
            if(i>0 && nums[i] == nums[i-1]){
                continue;
            }
            int j = i+1, k=n-1;
            while(j<k){
                int sum = a+nums[j]+nums[k];
                if(sum == target){
                    return sum;
                }
                ans = abs((long long)ans-target) > abs(sum-target) ? sum:ans;
                if(sum > target){
                    k--;
                    while(j<k && nums[k] == nums[k+1]){
                        k--;
                    }
                }else{
                    j++;
                    while(j<k && nums[j] == nums[j-1]){
                        j++;
                    }
                }
            }
        }
        return ans;
    }
};
```



##### Review1



##### Reflection

暴力解：时间复杂度、空间复杂度高



优化解：TODO ->  核心思想：固定一个数 + 双指针找另外两个数 + 去重 + 剪枝
